#include <stdio.h>

typedef int ElementType;

void bubble_sort(ElementType *array, int array_len)
{
    int i, j;
    int tmp;
    
    for (i = 0; i < array_len, ++i) {
        for (j = 0; j < ; ++j) {
            
        }
    }
}

int main()
{
    return 0;
}